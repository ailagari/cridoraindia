# Used by `npm run build:capacitor` (Android). Relative asset base required for WebView.
VITE_CAPACITOR_BUILD=true
# APK loads bundled dist/ (recommended). Set true only to point WebView at live production UI:
# VITE_CAPACITOR_LIVE_WEBVIEW=true
